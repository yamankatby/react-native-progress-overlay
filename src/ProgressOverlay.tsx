import React from 'react';
import { Text, View } from 'react-native';

interface Props {

}

const ProgressOverlay = (props: Props) => {
	return (
		<View>
			<Text>Hi, ProgressOverlay Component!</Text>
		</View>
	);
};

export default ProgressOverlay;
